<div class="coll-item-wrapper">
    {{#if organizeMode}}
        {{#if (not item.isGroup)}}
            <div class="coll-item-organize {{if selected 'item-selected'}}" {{action 'markSelected'}}>
                <i class="fa fa-check"></i>
                <p>Click to select</p>
            </div>
            {{/if}}
    {{/if}}
    <div class="coll-item coll-item-type-{{item.type}} {{if item.isGroup 'coll-item-isgroup'}}">
        <h3>{{#link-to 'collection.item' item.id}}{{item.title}}{{/link-to}}</h3>
        {{#if item.isGroup}}
            <p> Type: Group </p>
        {{else}}
            <p> Type: {{item.type}} </p>
            <p> Status: {{item.status}} </p>
        {{/if}}
    </div>
</div>
