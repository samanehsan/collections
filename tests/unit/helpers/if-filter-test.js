
import { ifFilter } from 'collections/helpers/if-filter';
import { module, test } from 'qunit';

module('Unit | Helper | if filter');

// Replace this with your real tests.
test('it works', function(assert) {
    const result = ifFilter([42]);
    assert.ok(result);
});

