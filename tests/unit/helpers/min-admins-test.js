
import { minAdmins } from 'collections/helpers/min-admins';
import { module, test } from 'qunit';

module('Unit | Helper | min admins');

// Replace this with your real tests.
test('it works', function(assert) {
    const result = minAdmins([42]);
    assert.ok(result);
});

