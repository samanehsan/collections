
import { permissionToRemoveContributors } from 'collections/helpers/permission-to-remove-contributors';
import { module, test } from 'qunit';

module('Unit | Helper | permission to remove contributors');

// Replace this with your real tests.
test('it works', function(assert) {
    const result = permissionToRemoveContributors([42]);
    assert.ok(result);
});

